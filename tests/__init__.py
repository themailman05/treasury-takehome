"""Test package for the AI Label Verification Prototype.

Present so the suite imports as a package and pytest's rootdir discovery is
stable; the repo itself uses a flat module layout at the root, which the tests
import directly (e.g. ``from verify_fields import ...``).
"""
